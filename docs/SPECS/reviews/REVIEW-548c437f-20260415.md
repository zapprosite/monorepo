# REVIEW-548c437f: prune perplexity-agent + Infisical SDK refs

**Commit:** `548c437f` — [chore(monorepo): prune perplexity-agent and Infisical SDK refs](https://github.com/will-zappro/monorepo/commit/548c437f)
**Date:** 2026-04-15
**Reviewer:** Claude Code
**Branch:** feature/quantum-helix-done

---

## Summary

Large cleanup commit removing `apps/perplexity-agent` (27 files), Infisical SDK refs from list-web/obsidian-web, updating governance docs, and adding hermes-gateway systemd hardening.

---

## Critical (Must Fix)

| File                           | Issue                                                                                                    | Fix                                                           |
| ------------------------------ | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| `.claude/scheduled_tasks.json` | JSON sintaxe corrompida — linhas 69-70 tinha `] }` duplicados. **Isto quebrou o husky pre-commit hook.** | ✅ Fixed: truncados para 68 linhas válidas. Commit `b3f2d1a2` |

---

## Important (Should Fix)

| File             | Issue                                                     | Suggestion                                                        |
| ---------------- | --------------------------------------------------------- | ----------------------------------------------------------------- |
| `pnpm-lock.yaml` | Adicionado 10,170 linhas. Lock file legítimo mas massivo. | Verificar se .gitignore tem `pnpm-lock.yaml` — se não, adicionar. |

---

## Suggestions (Consider)

| File                                   | Suggestion                                                                                                                                                                                          |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/GUIDES/INFISICAL-SDK-PATTERN.md` | Este ficheiro ainda tem exemplos `InfisicalClient` em código. Embora seja docs/guidance, pode ser confundido com código real. Considerar marcar como DEPRECATED se o padrão agora é .env canonical. |
| `hermes-gateway.service` (systemd)     | Mudança de `Restart=on-failure` para `Restart=always` foi correta mas é um change behaviour — documentar em SPEC-CHANGE-LOG.                                                                        |

---

## Verdict

- **Approved** — com fix do scheduled_tasks.json (já aplicado)
- ⚠️ Husky hook foi bypassado com `--no-verify` para este commit — na próxima vez garantir JSON válido antes de commit

---

## Files Changed

48 files: 10,479 insertions(+), 7,107 deletions(-)

## Review Findings

- ✅ Prune perplexity-agent: completo (27 files removidos)
- ✅ Infisical SDK refs removidos de apps/ — 0 SDK em código de aplicação
- ✅ pipeline.json atualizado — 7 tasks removidas
- ✅ hermes-gateway systemd: Restart=always + WatchdogSec=300
- ⚠️ JSON syntax error em scheduled_tasks.json (já fixado)
- ⚠️ Husky bypassado (JSON inválido)
