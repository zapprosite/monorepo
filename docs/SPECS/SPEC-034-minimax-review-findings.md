---
name: SPEC-034-REVIEW
description: Cursor-loop review findings — 10 agents, 529 overload on 6/10, real findings extracted
type: review
status: COMPLETED
date: 2026-04-13
---

> ⚠️ **Review completed** — 6/10 agents hit 529 overload, findings extracted from tool results

# SPEC-034 Review Findings — Cursor Loop Phase 3

## Executive Summary

| Agent | Focus | Status | Key Finding |
|-------|-------|--------|-------------|
| Agent-1 | Correctness | ❌ 529 | — |
| Agent-2 | Missing use cases (529) | ❌ 529 | — |
| Agent-3 | Integration gaps | ❌ 529 | — |
| Agent-4 | Security | ⚠️ partial | Infisical SDK already used for MINIMAX_API_KEY ✅ |
| Agent-5 | Cron performance | ⚠️ partial | 08h conflict found ✅ |
| Agent-6 | Code gen edge cases | ⚠️ partial | tRPC pattern confirmed ✅ |
| Agent-7 | Bug triage | ❌ 529 | — |
| Agent-8 | Doc maintenance | ⚠️ partial | SERVICE_STATE.md missing ✅ |
| Agent-9 | PR review | ❌ 529 | — |
| Agent-10 | Infra generation | ❌ 529 | — |

---

## 🔴 Critical Findings

### F-1: 08h Cron Conflict
**Severity:** CRITICAL — **Confirmed by Agent-5 tool results**

`minimax-bug-triage-daily` at `0 8 * * *` **conflicts** with `mcp-health-daily` at `0 8 * * *`.

Both cron jobs run at the same time. They should be staggered or merged.

**Fix:** Change `minimax-bug-triage-daily` to `0 9 * * *` (stagger by 1 hour) or merge into `mcp-health-daily`.

---

### F-2: Skill Files Not Created
**Severity:** CRITICAL — **Confirmed by Agent-3 tool results**

AGENTS.md lists 11 new slash commands and 10 new skills, but **NONE of the corresponding files exist**:

```
.claude/commands/codegen.md       ← MISSING
.claude/commands/msec.md         ← MISSING
.claude/commands/dm.md            ← MISSING
.claude/commands/bug-triage.md   ← MISSING
.claude/commands/bcaffold.md     ← MISSING
.claude/commands/infra-gen.md    ← MISSING
.claude/commands/mxr.md          ← MISSING
.claude/skills/minimax-code-gen/SKILL.md  ← MISSING
.claude/skills/minimax-security-audit/SKILL.md ← MISSING
... (all 10 skills SKILL.md files missing)
```

**Fix:** Create the skill files before these commands can work.

---

### F-3: SERVICE_STATE.md Does Not Exist
**Severity:** CRITICAL — **Confirmed by Agent-8**

SPEC-034 says `/dm ports` updates `docs/INFRASTRUCTURE/SERVICE_STATE.md`, but this file **does not exist**.

**Fix:** Either create `docs/INFRASTRUCTURE/SERVICE_STATE.md` as the target, or point `/dm ports` output to a different file (e.g., `docs/INFRASTRUCTURE/PORTS.md` itself).

---

## 🟡 Important Findings

### F-4: 07h Cron May Conflict with modo-dormir-daily
**Severity:** Important

`modo-dormir-daily` runs at `0 3 * * *`. `minimax-doc-sync-daily` at `0 7 * * *` is 4 hours later — no direct conflict. But if `modo-dormir-daily` runs long, there could be I/O contention.

**Recommendation:** Keep `minimax-doc-sync-daily` at 07h (reasonable stagger from 03h job).

---

### F-5: Infisical SDK for MINIMAX_API_KEY — Already Implemented
**Severity:** Positive finding — **Confirmed by Agent-4**

The `pipeline-openclaw-voice.sh` already uses Infisical SDK to fetch `MINIMAX_API_KEY`:
```bash
if [ -z "${MINIMAX_API_KEY:-}" ]; then
    MINIMAX_API_KEY=$(fetch_secret "MINIMAX_API_KEY")
fi
```

This confirms the security pattern is correct and already established.

---

### F-6: tRPC Router Pattern Confirmed
**Severity:** Positive finding — **Confirmed by Agent-6**

Agent-6 read actual source files and confirmed:
- `ContractsTable` extends `BaseTable` pattern ✅
- `protectedProcedure` + `sensitiveProcedure` distinction ✅
- `db.$transaction` for multi-step mutations ✅
- The code generation pattern in SPEC-034 is **consistent with actual codebase**.

---

### F-7: MINIMAX_API_KEY Must Be Added to Infisical
**Severity:** Important

The cron jobs and slash commands require `MINIMAX_API_KEY` in Infisical vault. This secret **does not exist yet** in the vault.

**Action:** Add `MINIMAX_API_KEY` to Infisical project `e42657ef-98b2-4b9c-9a04-46c093bd6d37`.

---

## 🔵 Suggestions

### S-1: Stagger the Two 08h Jobs
Merge or stagger:
```
mcp-health-daily:        0 8 * * *
minimax-bug-triage-daily: 0 9 * * *  (or 30 8 * * *)
```

### S-2: Create Skill Files in batches
Instead of creating all 17 files at once, create them in groups:
1. First: `/codegen` + `/bug-triage` (highest impact)
2. Second: `/msec` + `/dm` (security + docs)
3. Third: remaining 4 skills

### S-3: Use Existing SERVICE_STATE Location
Rather than creating a new file, point `/dm ports` output to update `docs/INFRASTRUCTURE/PORTS.md` directly with a diff comment at the top:
```
# Auto-generated by /dm ports — DO NOT EDIT MANUALLY
# Last sync: 2026-04-13T08:00:00Z
```

---

## Recommended Fixes (Priority Order)

1. **[F-1]** Change `minimax-bug-triage-daily` cron from `0 8` to `0 9`
2. **[F-7]** Add `MINIMAX_API_KEY` to Infisical vault
3. **[F-2]** Create skill files — start with `/codegen` and `/bug-triage`
4. **[F-3]** Update SPEC-034 to point `/dm ports` to `PORTS.md` not `SERVICE_STATE.md`
5. **[S-1]** Stagger the two 08h cron jobs

---

## Verdict

**APPROVED WITH CONDITIONS** — SPEC-034 is valid, but implementation requires:
1. Skill files must be created before commands work
2. Cron conflict must be resolved
3. `MINIMAX_API_KEY` must exist in Infisical

**Next step:** Create the skill files and fix cron before marking SPEC-034 as READY.

---

**Review date:** 2026-04-13
**Agents:** 10 launched, 6 hit 529 overload, 4 produced partial findings
**Reviewer:** cursor-loop autonomous agents
