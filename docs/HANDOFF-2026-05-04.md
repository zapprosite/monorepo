# Context Handoff — Monorepo Prune + Backend Reduction

**Saved:** 2026-05-04T15:32:00-03:00  
**Branch:** `feature/nano-blade`  
**Last commit:** `ccc72b8` — chore: aggressive monorepo prune (SPEC-300)

---

## O que já foi feito (não repetir)

### Poda Agressiva (SPEC-300) — COMPLETO
- 6 apps removidas: `perplexity-agent`, `hvac-manual-downloader`, `list-web`, `obsidian-web`, `painel-organism`, `orchestrator`
- `obsidian/` apagado (mirror de docs/)
- `apps/archived/` → `archive/` na raiz
- SPECs mortas → `docs/SPECS-dead/` (`SPEC-115`, `SPEC-006-playwright-e2e`)
- `.env` e `.env.example` limpos: removido `OPENROUTER_MODEL`, `OPENROUTER_API_BASE`, `STT_PROXY_URL`, `WHISPER_MEDIUM_PORT`, `OLLAMA_URL` (consolidado em `OLLAMA_BASE_URL`)
- `AI_GATEWAY_PORT` renomeado para `VOICE_GATEWAY_PORT`
- `README.md` reescrito em 1 tela (~45 linhas)
- `monorepo-TREE.md` atualizado com estrutura pós-poda
- `apps/monitoring/` movido para `config/monitoring/`
- `biome.json` com excludes para symlinks (`hermes`, `ops`, `edge-tts`, etc.)
- `pnpm-lock.yaml` regenerado

### Gateways consolidados
- **LiteLLM :4018/v1** — único gateway LLM (hermes-* aliases)
- **Voice Gateway :4002** — TTS (Edge-tts :8012) + STT (Groq cloud)

### Builds
- `pnpm build --filter=@connected-repo/frontend` ✅
- `pnpm build --filter=@repo/ai-gateway` ✅
- `pnpm build --filter=@connected-repo/backend` ❌ (PREEXISTENTE — 190 erros TS OrchidORM)

---

## O que FALTA fazer (próxima sessão)

### Alto prioridade
1. **Revisar e aprovar SPEC-301** — `docs/SPECS/SPEC-301-backend-crm-reducao-arquitetural.md`
   - Proposta: Factory Pattern `createCrudRouter()` para reduzir ~35% do backend
   - Decisão pendente: Aprovar Abordagem B (Factory) vs A (Conservative) vs C (Radical)

2. **Corrigir build do backend** — erros TS2742 em OrchidORM são preexistentes
   - NÃO rodar `biome check --write` em `apps/api/` (quebra `@ts-ignore` → `@ts-expect-error`)
   - Foco: tipos OrchidORM `pqb/internal` não exportados

3. **Implementar SPEC-301** (se aprovado)
   - Criar `@backend/lib/crud-router.factory.ts`
   - Migrar módulos simples primeiro: `prompts`, `reminders`, `mcp-connectors`

### Média prioridade
4. **Coolify cleanup** — verificar projetos/recursos órfãos
   - API não respondeu na sessão anterior — verificar endpoint/auth token
   - Apps removidas provavelmente não tinham projetos Coolify ativos

5. **Google OAuth redirects legados** — `GOOGLE_REDIRECT_URI_LIST_WEB`, `GOOGLE_REDIRECT_URI_OBSIDIAN` podem ser removidos do `.env`

### Baixa prioridade
6. **Biome check nos arquivos seguros** — `apps/ai-gateway/`, `docs/`, `scripts/` (excluir `apps/api/` até build passar)

---

## Arquivos chave

| Arquivo | Propósito |
|---------|-----------|
| `docs/SPECS/SPEC-300-monorepo-minimo-viavel-prune-total.md` | SPEC da poda (concluído) |
| `docs/SPECS/SPEC-301-backend-crm-reducao-arquitetural.md` | SPEC da redução arquitetural (draft, aguardando aprovação) |
| `AGENTS.md` | Source of truth atualizado |
| `README.md` | Overview 1 tela |
| `.env` / `.env.example` | Vars limpas (revisar se mais cleanup necessário) |
| `biome.json` | Excludes fixados |

---

## Riscos e gotchas

- **Build backend quebrado é PREEXISTENTE** — não foi causado pela poda. Não gastar tempo tentando "desfazer" a poda para consertar.
- **Não rodar biome --write em apps/api/** — a regra `suspicious/noTsIgnore` converte `@ts-ignore` → `@ts-expect-error`, que quebra o build porque TS2742 não ocorre em todos os arquivos (alguns compilam sem erro).
- **Branch atual é `feature/nano-blade`** — merge para `main` só após build passar.

---

## Prompt de reinício (copie e cole)

```
Continuar trabalho no monorepo. Contexto anterior:
- Branch: feature/nano-blade
- Commit: ccc72b8 (SPEC-300 poda agressiva concluída)
- 6 apps removidas, gateways consolidados, .env limpo
- Build web e ai-gateway passam. Build backend quebrado PREEXISTENTE (OrchidORM TS2742)
- SPEC-301 draftado em docs/SPECS/SPEC-301-backend-crm-reducao-arquitetural.md (aguardando aprovação)

Próximos passos prioritários:
1. Revisar SPEC-301 e decidir abordagem (A=conservative, B=factory pattern, C=radical)
2. Se aprovado, implementar createCrudRouter() para reduzir boilerplate backend
3. Investigar e documentar (não necessariamente corrigir agora) os erros TS2742 preexistentes
4. Coolify cleanup — verificar se há projetos órfãos

NÃO rodar biome check --write em apps/api/ (quebra build).
```
