OPENWEBUI-HVAC VERSION 1.0.1
=============================

Locked Configuration: services/docker-compose.openwebui.yml

CHANGELOG
---------
2026-05-01 v1.0.1 - CRITICAL FIX
- Changed REDIS_HOST from zappro-redis to trieve-redis
- zappro-redis requires authentication (no password found in .env)
- trieve-redis is open (no AUTH required)
- OpenWebUI now connects to trieve-redis on zappro-lite_default network
- Root cause: .env REDIS_PASSWORD didn't match zappro-redis config

2026-05-01 v1.0.0
- Initial locked configuration
- Uses zappro-redis for sessions (not localhost)
- Points OPENAI_API_BASE_URL to host.docker.internal:4017 (HVAC pipeline)
- Joins zappro-lite_default network for Redis access
- Google OAuth enabled for authentication

APPROVAL
--------
Platform Engineering - 2026-05-01

VERIFICATION
------------
Run: ./scripts/verify-service-configs.sh

ROLLBACK
--------
git checkout HEAD~1 -- services/docker-compose.openwebui.yml
docker compose -f services/docker-compose.openwebui.yml up -d

LESSONS LEARNED
---------------
1. Never use localhost for inter-container communication
2. Always verify Redis connectivity before deploying
3. Test authentication credentials match between services
4. Document network membership explicitly
5. Use locked configs with version control