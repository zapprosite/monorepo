{
  "name": "@repo/app-template",
  "version": "0.0.1",
  "private": true,
  "description": "App template for monorepo",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "turbo run build",
    "start": "node dist/index.js",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint ."
  },
  "dependencies": {
    "@repo/zod-schemas": "workspace:*",
    "fastify": "^5.0.0",
    "@trpc/server": "^11.7.0",
    "zod": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0",
    "vitest": "^2.0.0",
    "biome": "^2.0.0"
  }
}
