# Plan 05-02: Refatoração hvac_rag_pipe.py - Busca Dual

## Goal
Migrar o orquestrador de busca para o padrão de dois estágios: primeiro localiza a intenção no FAQ Index, depois recupera a prova real no Raw Manual Index.

## Proposed Changes

### HVAC Engine (Scripts)

#### [MODIFY] [hvac_rag_pipe.py](file:///srv/monorepo/scripts/hvac-rag/hvac_rag_pipe.py)
- Renomear `search_qdrant` para `search_qdrant_raw`.
- Criar `search_qdrant_faq`.
- Implementar `orchestrate_dual_search(query)`:
  1. `faq_results = search_qdrant_faq(query)`
  2. `top_intent = faq_results[0]`
  3. `evidence = search_qdrant_raw(query, filter={"manual_id": top_intent.manual_id})`

## Verification Plan

### Automated Tests
- `python3 scripts/hvac-rag/hvac_rag_pipe.py --query "Como testar sensor de descarga LG Inverter?"`
- Verificar se a resposta contém a citação da página correta baseada no FAQ.
