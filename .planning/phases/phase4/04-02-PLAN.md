---
phase: 04-reconhecimento-placas-componentes-foto
plan: 02
type: execute
wave: 1
files_modified:
  - scripts/hvac-rag/hvac_field_memory.py
  - scripts/hvac-rag/hvac_memory_context.py
  - tests/hvac-rag/test_memory_integration.py
autonomous: true
requirements:
  - VISION-02
---

# Plan 04-02 - integração do vision output com memória/contexto de diagnóstico

<objective>
Garantir que os dados estruturados extraídos de fotos de PCB (Fase 04-01) sejam persistidos na memória da conversa e utilizados para enriquecer o contexto do tutor, permitindo diagnósticos baseados em evidências visuais.
</objective>

## Tasks

<task type="auto" tdd="true">
<name>Task 1: Persistência de Visão na Memória</name>
<files>scripts/hvac-rag/hvac_field_memory.py, tests/hvac-rag/test_memory_integration.py</files>

1. Criar `tests/hvac-rag/test_memory_integration.py` com teste para `update_state` contendo dados de visão.
2. Modificar `hvac_field_memory.py` para garantir que o método `update_state` aceite e armazene campos de visão no `jsonb`.
</task>

<task type="auto" tdd="true">
<name>Task 2: Injeção de Visão no Contexto</name>
<files>scripts/hvac-rag/hvac_memory_context.py, tests/hvac-rag/test_memory_integration.py</files>

1. Adicionar teste que valida se a função de construção de contexto inclui informações da placa/componentes se presentes no estado.
2. Modificar `hvac_memory_context.py` para incluir uma seção `[VISUAL EVIDENCE]` no prompt se houver dados de visão no estado da conversa.
</task>

## Verification

```bash
python3 -m pytest tests/hvac-rag/test_memory_integration.py -v
```
