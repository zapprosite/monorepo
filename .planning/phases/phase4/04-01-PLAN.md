---
phase: 04-reconhecimento-placas-componentes-foto
plan: 01
type: execute
wave: 1
files_modified:
  - scripts/hvac-rag/hvac_vision.py
  - tests/hvac-rag/test_vision.py
autonomous: true
requirements:
  - VISION-01
---

# Plan 04-01 - contratos e estado canônico para fotos de PCB/componentes

<objective>
Fortalecer `hvac_vision.py` para o caso de fotos de PCB/componentes HVAC, com heurística confiável de classificação, prompt estável e state update completo para consumo do tutor.
</objective>

## Tasks

<task type="auto" tdd="true">
<name>Task 1: Cobrir e corrigir o fluxo PCB/componentes</name>
<files>scripts/hvac-rag/hvac_vision.py, tests/hvac-rag/test_vision.py</files>

1. Adicionar testes para:
   - classificação de PCB por hints técnicos
   - prompt PCB com campos estruturados obrigatórios
   - parsing de JSON em fenced block
   - state update de PCB com board type, labels e conectores
2. Corrigir heurística para não classificar `placa inverter` como `nameplate`.
3. Enriquecer `state_update_from_vision()` para expor os campos estruturados de PCB.
</task>

## Verification

```bash
python3 -m pytest tests/hvac-rag/test_vision.py -q
python3 -m pytest tests/hvac-rag/ -q
```
