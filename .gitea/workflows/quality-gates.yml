name: Quality Gates

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  governance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run governance quality gates
        run: PLACEHOLDER_DEBT_ENFORCE=1 bash scripts/quality-gates.sh
