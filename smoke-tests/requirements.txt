# requirements.txt — Hermes Telegram Smoke Test (SPEC-069)
# Instalar: pip install -r smoke-tests/requirements.txt

# Core testing
pytest>=8.0
pytest-asyncio>=0.23

# Telegram Bot API client
python-telegram-bot>=21.0

# HTTP client (for health checks)
aiohttp>=3.9
