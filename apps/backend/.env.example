# Node Environment
NODE_ENV=development

# Server Port (porta 4000 está ocupada pelo LiteLLM — usar 4001)
PORT=4001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=connected_repo_db

WEBAPP_URL=http://localhost:5173
VITE_API_URL=http://localhost:4001

# Allowed Origins for CORS
ALLOWED_ORIGINS=http://localhost:5173

# Google OAuth Credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Session Secret (minimum 32 characters - generate with: openssl rand -base64 32)
SESSION_SECRET=your-secret-key-here-min-32-chars-use-openssl-rand-base64-32

# Internal API Secret (minimum 32 characters - used for internal endpoints like webhook processor)
# Generate with: openssl rand -base64 32
INTERNAL_API_SECRET=your-internal-secret-key-here-min-32-chars-use-openssl-rand-base64-32
