{
	"name": "@connected-repo/orchestrator",
	"version": "0.1.0",
	"description": "Universal Orchestration System with human gates, agent pool, MCP adapters, and webhook integration",
	"main": "src/index.ts",
	"type": "module",
	"scripts": {
		"check-types": "tsc --noEmit",
		"build": "npx -p typescript tsc && npx -p typescript tsc-alias",
		"dev": "tsx watch src/index.ts",
		"start": "node dist/index.js"
	},
	"dependencies": {
		"@connected-repo/zod-schemas": "*",
		"@trpc/server": "^11.7.0",
		"yaml": "^2.7.0",
		"zod": "^4.1.12"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"tsx": "^4.19.0",
		"typescript": "5.9.3"
	}
}
