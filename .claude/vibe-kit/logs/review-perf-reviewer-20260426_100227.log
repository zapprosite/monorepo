Performance review complete. Report written to `/srv/monorepo/docs/REVIEW-perf-reviewer-2026-04-26.md`.

**Summary of findings:**

| Severity | Count |
|----------|-------|
| CRITICAL | 2 (journal entries + webhook N+1) |
| HIGH | 4 (blocking sync I/O, missing pagination) |
| MEDIUM | 6 (kanban client-side filtering, broken cache expiry, RAG sync I/O) |
| LOW | 3 |

**Top 4 blocking issues:**
1. Journal entries — no pagination (unbounded memory/network)
2. Webhook processor — N+1 (51 DB queries per 50-webhook batch) + sequential HTTP
3. **NEW:** Audio transcription — `fs.writeFileSync`/`readFileSync` blocks event loop
4. Equipment + prompts + subscriptions — missing pagination (growth risk)
