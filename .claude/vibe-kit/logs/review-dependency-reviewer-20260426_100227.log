Relatório gerado em `/srv/monorepo/docs/REVIEW-dependency-reviewer-2026-04-26.md`.

**Resumo das findings:**

1. **CRÍTICO** — `apps/ai-gateway` usa `zod@^3.24.1` (v3) enquanto `packages/zod-schemas` exige `zod@^4.1.12` (v4) como peer dependency. Conflito de versão com potencial de quebra.

2. **ALTO** — `tsc@^2.0.4` no root package.json é um pacote deprecated e redundante (TypeScript já está em devDependencies). Não é usado em nenhum build script.

3. **MÉDIO** — 8 devDependencies desatualizadas (biome, typescript-eslint, eslint, globals, lint-staged, typescript). Nenhuma vulnerabilidade encontrada pelo `pnpm audit`.

4. **MÉDIO** — `packages/config` só contém meta-package sem dependencies reais.

5. **BAIXO** — Vibe-kit state indica "looping" com task "idle" — loop ocioso sem trabalho pendente.

6. **BAIXO** — `apps/monitoring/` existe como diretório mas não tem `package.json`, não sendo um workspace package.
